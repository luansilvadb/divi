# Tasks: Harmonize SettingsItem Hover with Dropdown Background

## 1. Style Refinement
- [x] 1.1 Update `src/components/molecules/SettingsItem.vue` to change the light-mode hover background-color to `#f0f2f5`.
- [x] 1.2 Update `src/components/molecules/SettingsItem.vue` to change the dark-mode hover background-color to `#2a2a2a`.

## 2. Validation
- [x] 2.1 Confirm in the UI that hovering over a settings row (like "Modo de tema") creates a background that perfectly matches the dropdown input inside it.
- [x] 2.2 Verify that the `q-ripple` still provides a subtle tactile response over the solid color.
- [x] 2.3 Verify transition smoothness.
