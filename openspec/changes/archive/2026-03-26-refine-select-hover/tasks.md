# Tasks: Refine Select Hover State

## 1. CSS Implementation
- [x] 1.1 In `BaseSelect.vue`, update the global `.custom-select-menu` style for `.q-item`.
- [x] 1.2 Add explicit hover and focused styles using `#f0f2f5` (Light) and `#2a2a2a` (Dark) background colors.
- [x] 1.3 Add `transition: background 0.2s ease` to `.q-item`.
- [x] 1.4 Refine `.q-item--active` color to ensure it matches the light/dark mode requirements.

## 2. Validation
- [x] 2.1 Check if the hover background is too close to the menu background or just the right "preview" feel.
- [x] 2.2 Verify dark mode transitions for the dropdown.
- [x] 2.3 Verify that text color remains high-contrast across all states.
